#include "test_example_functions.h"

/*std::tuple<std::vector<std::string>, DocumentStatus> MatchDocument(SearchServer& test,const std::string_view& raw_query, int document_id) {
    LOG_DURATION_STREAM("Operation time", std::cout);
    return test.MatchDocument(raw_query, document_id);
}

std::vector<Document> FindTopDocuments(SearchServer& test,const std::string_view& raw_query) {
    LOG_DURATION_STREAM("Operation time", std::cout);
    return test.FindTopDocuments(raw_query);
}

void AddDocument(SearchServer& test, int document_id, const std::string_view& document, DocumentStatus status,
                     const std::vector<int>& ratings){
    LOG_DURATION("Operation time");
    test.AddDocument(document_id, document, status, ratings);
}*/